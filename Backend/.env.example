# Comma-separated origins are not used here - edit app/core/config.py's
# `cors_origins` list directly, or switch to a comma-split env var if you
# prefer. Left as a placeholder for a future SIGNSYNC_-prefixed .env.
